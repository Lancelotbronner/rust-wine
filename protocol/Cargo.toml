[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = { workspace = true }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
const_format = "0.2.36"
