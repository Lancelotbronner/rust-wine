# Rusty Wine

This is an in-progress proof of concept for Wine in Rust.

Wine is a program which allows running Microsoft Windows programs (including DOS, Windows 3.x, Win32, and Win64 executables) on Unix.

It consists of a program loader which loads and executes a Microsoft Windows binary.

This project is not intended to be identical or compatible with Wine.
